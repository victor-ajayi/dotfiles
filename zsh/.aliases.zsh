# Custom aliases
alias python=python3
alias activate="source .venv/bin/activate"
alias pip="pip3"
alias createenv="python -m venv .venv && activate"
alias zconfig="code ~/.zshrc"
alias mkdir="mkdir -pv"
alias cls="clear"